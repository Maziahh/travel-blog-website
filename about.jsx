//create a folder called about and then a file called page.jsx inside the about folder and put the code inside page.jsx

export default function about() {
    return (
        <div>
            <h1>About us</h1>
        </div>
    );
}
